let a=-10;
// The below shows to positive if it is negative number.
console.log(Math.abs(a));

// The below shows power of that number.
let x=2,y=3;
console.log(Math.pow(x,y));

//nearest smallest integer value.
let z=5.847759;
console.log(Math.floor(z));

//nearest largest integer value
console.log(Math.ceil(z));

//gives random number. from 0 to <(less than 1)
console.log(Math.random());
